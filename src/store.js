import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {
  topics: [],
}

const mutations = {
}

const actions = {
}

const getters = {
}

export default new Vuex.Store({ state, actions, mutations, getters })
